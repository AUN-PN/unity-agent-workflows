interface:
  display_name: "Unity Agent Workflows"
  short_description: "Unity routing, runtime proof, and validation"
  default_prompt: "Use $unity-agent-workflows to route Unity work and require runtime numeric proof for repeated visible-output failures."
policy:
  allow_implicit_invocation: true
